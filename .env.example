# ==============================================================================
# CORTEX - Environment Variables Template
# ==============================================================================
# IMPORTANT: Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!
# ==============================================================================

# ==============================================================================
# DJANGO CONFIGURATION
# ==============================================================================

# Django Secret Key (Generate with: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())')
SECRET_KEY=django-insecure-CHANGE-ME-IN-PRODUCTION-min-50-characters-long

# Debug Mode (ALWAYS False in production)
DEBUG=True

# Allowed Hosts (comma-separated, no spaces)
# Production example: .railway.app,.onrender.com,cortexnhl.com,www.cortexnhl.com
ALLOWED_HOSTS=localhost,127.0.0.1

# ==============================================================================
# DATABASE (Supabase PostgreSQL)
# ==============================================================================

# Supabase Connection String (from Supabase Dashboard → Settings → Database)
# Format: postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
DATABASE_URL=postgresql://user:password@host:port/database

# ==============================================================================
# STRIPE PAYMENT CONFIGURATION
# ==============================================================================

# Stripe Publishable Key (starts with pk_test_ or pk_live_)
STRIPE_PUBLIC_KEY=pk_test_xxxxx

# Stripe Secret Key (starts with sk_test_ or sk_live_)
STRIPE_SECRET_KEY=sk_test_xxxxx

# Stripe Webhook Secret (from Stripe Dashboard → Developers → Webhooks)
STRIPE_WEBHOOK_SECRET=whsec_xxxxx

# Stripe Price ID for Premium Subscription (from Stripe Dashboard → Products)
STRIPE_PRICE_ID=price_xxxxx

# ==============================================================================
# EMAIL CONFIGURATION (Optional - for production)
# ==============================================================================

# Email Backend (console for dev, smtp for production)
# Uncomment and configure for production:
# EMAIL_HOST=smtp.sendgrid.net
# EMAIL_PORT=587
# EMAIL_HOST_USER=apikey
# EMAIL_HOST_PASSWORD=your-sendgrid-api-key
# DEFAULT_FROM_EMAIL=noreply@cortexnhl.com

# ==============================================================================
# NOTES
# ==============================================================================
# 
# Development Setup:
# 1. Copy this file: cp .env.example .env
# 2. Update DATABASE_URL with your Supabase credentials
# 3. Add Stripe test keys from https://dashboard.stripe.com/test/apikeys
# 4. Keep DEBUG=True for local development
#
# Production Setup (Railway/Render):
# 1. Set DEBUG=False
# 2. Generate new SECRET_KEY
# 3. Update ALLOWED_HOSTS with your domain
# 4. Use Stripe live keys (pk_live_, sk_live_)
# 5. Configure email SMTP (SendGrid, Mailgun, etc.)
#
# Security:
# - NEVER commit .env to Git (.gitignore should block it)
# - Use different SECRET_KEY for production
# - Rotate Stripe webhook secret if exposed
# - Use environment variables in cloud platforms (Railway/Render)
#
# ==============================================================================
